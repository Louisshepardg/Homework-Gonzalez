//prevent default//$(function() {////      $('#assignment6Form').on('submit', function(event){//    event.preventDefault();//        });//});//console.log('hello world');//display the warning message//var firstItem = document.getElementById("container2");//firstItem.className = 'show';//remove the warning message//if (firstItem.hasAttribute('class')){//    firstItem.removeAttribute('class');//}function checkFirstname() {                             // Declare function    var elMsg = document.getElementById('feedback1');     // Get feedback element    if (this.value.length < 2) {                         // If username too short        elMsg.textContent = 'Name must be 2 characters or more'; // Set msg    } else {                                             // Otherwise        elMsg.textContent = '';                            // Clear msg    }}var elName = document.getElementById('Name');  // Get name input// When it loses focus call checkFirstname()elName.addEventListener('blur', checkFirstname, false);////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////function checkSurname() {                             // Declare function    var elMsg = document.getElementById('feedback2');     // Get feedback element    if (this.value.length < 2) {                         // If surrname too short        elMsg.textContent = 'Surname must be 2 characters or more'; // Set msg    } else {                                             // Otherwise        elMsg.textContent = '';                            // Clear msg    }}var elSurname = document.getElementById('Surname');  // Get name input// When it loses focus call checkSurname()elSurname.addEventListener('blur', checkSurname, false);////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////function checkUsername() {                             // Declare function    var elMsg = document.getElementById('feedback');     // Get feedback element    if (this.value.length < 5) {                         // If username too short        elMsg.textContent = 'Username must be 5 characters or more'; // Set msg    } else {                                             // Otherwise        elMsg.textContent = '';                            // Clear msg    }}var elUsername = document.getElementById('username');  // Get username input// When it loses focus call checkUsername()elUsername.addEventListener('blur', checkUsername, false);////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////function checkPass(){    //Store the password field objects into variables ...    var pass1 = document.getElementById('pass1');    var pass2 = document.getElementById('pass2');    //Store the Confimation Message Object ...    var message = document.getElementById('confirmMessage');    //Set the colors we will be using ...    var goodColor = "#66cc66";    var badColor = "red";    //Compare the values in the password field    //and the confirmation field    if(pass1.value == pass2.value){        //The passwords match.        //Set the color to the good color and inform        //the user that they have entered the correct password        pass2.style.backgroundColor = goodColor;        message.style.color = goodColor;        message.innerHTML = "Passwords Match!"    }else{        //The passwords do not match.        //Set the color to the bad color and        //notify the user.        //pass2.style.backgroundColor = badColor;        message.style.color = badColor;        message.innerHTML = "Passwords Do Not Match!"    }}//create  a new div element with a message//var newEl = document.createElement('div');////var newText = document.createTextNode('Field must be filled in with at least 5 characters');////newEl.appendChild(newText);////var position = document.getElementsByTagName('Form')[0];////position.appendChild(newEl);//here are the notes from class//function checkLength ( string ,  length_to_check ) {////    // functions can have return values////    if ( string.length >= length_to_check )//    {//        return true ;//    }//    else//    {//        return false ;//    }////}////// attach single submit event to the form,////var ourForm = document.getElement() ;//ourForm.addEventListener ( 'submit', validate ) ;////function validate ( ) {////    var message ;////    // loop through all of the elements//    // manually iterating over each element////    // validate first name//    var firstname = ... ;//    checkLength ( firstname, 2 ) ;////    var lastname = ... ;//    checkLength ( lastname, 2 ) ;////// message += 'Your last name is blank.' ;////    var username = ... ;//    username_length_return_value = checkLength ( lastname, 7 ) ;////    if ( ! username_length_return_value )//    {////        message += 'Your username is too short.' ;//        // highlight the given field with a red border, red background, etc.//    }////    alertArea.innerHTML = message ;////    return false ;//}