//prevent default// $(function() {//       $('#assignment6').on('submit', function(e){//     e.preventDefault();//         });// });//change input border using focus event// $('input').not('#submit').on('focus', function(){//     $(this).addClass('focus');// }); //Why does this work?//function to add a red or green border depending on whether input is//validated  $('#assignment6').on('submit',function(event){		// run a check to see if a given field is not blank	// two kinds of checks: global (on every field) and local (custom check)});// $('input#name').on('blur',function(){// 	console.log("input name value: " + $("input#name").val());// 	var $inPut = $("input#name");// 	var $inPutname = $inPut.val();// 	var $inPutlength = $inPutname.length;// 	// console.log("input value length: " + $inPutlength);// 	// only run this check if the field isn't empty!// 	if ( $inPutlength != 0 ) {// 		if ( $inPutlength < 2 ) {			// 			$inPut.removeClass('focus').addClass('fail');// 		}// 		else {// 			if ( $inPut.hasClass( 'fail' ) ) {// 				$inPut.removeClass('fail') ;// 			}			// 			$inPut.removeClass('focus').addClass('pass');// 		}// 	}// 	else {// 		$inPut.removeClass('focus') ;// 	}// });//code block to give specific feedback if input isnt valid (0 characters added)// var reSponse = $('input#name');// $(function(){// if (reSponse.length < 2){//     $('ul#feedback li#one').removeClass('off').addClass('on');// }// });// var reSponse2 = $('input#surname');// $(function(){// if (reSponse2.length < 2){//     $('ul#feedback li#two').removeClass('off').addClass('on');// }// });// var reSponse3 = $('input#username');//  $(function(){// if (reSponse3.length < 7){//  $('ul#feedback li#three').removeClass('off').addClass('on');// }// });// var reSponse4 = $('input#password');// $(function(){// if (reSponse4.length < 8){//     $('ul#feedback li#four').removeClass('off').addClass('on');// }// });//attempt to remove feedback if it's valid// (function() {//         var validResponse = $('input').text;//     if(validResponse.length >= 2){//         $('ul#feedback li(0)').removeClass('on').addClass('off');//     }//     });